server:
  port: 8889
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hr
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false